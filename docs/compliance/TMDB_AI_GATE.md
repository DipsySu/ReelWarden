# COMPLIANCE-TMDB-AI Gate

Default status: `blocked`.

Runtime rule: when TMDB and AI are both enabled and this gate is not `accepted`, ReelWarden must reject the configuration with `COMPLIANCE_PROVIDER_AI_COMBINATION_BLOCKED`.
